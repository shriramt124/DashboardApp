<UserControl
    x:Class="DashboardApp.Controls.GlobalLeftSidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="60"
    Background="{StaticResource SidebarBackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="SidebarToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextColorSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="BackgroundBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Margin="4">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="BackgroundBorder"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="{StaticResource HighlightColor}"
                                                            Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="IconPresenter"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource BrandPrimaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundBorder"
                                                                           Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource HighlightBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="CheckedPointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="IconPresenter"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource BrandPrimaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundBorder"
                                                                           Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource HighlightBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter x:Name="IconPresenter"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="5" Margin="0,10,0,20">
            <Image Source="/Assets/Square44x44Logo.png" Width="32" Height="32"/>
            <TextBlock Text="Prave"
                       Foreground="{StaticResource TextColorPrimaryBrush}"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="5">
            <ToggleButton Style="{StaticResource SidebarToggleButtonStyle}" ToolTipService.ToolTip="Dashboards">
                <FontIcon Glyph="&#xE771;"/>
            </ToggleButton>

            <ToggleButton Style="{StaticResource SidebarToggleButtonStyle}" ToolTipService.ToolTip="Data" IsChecked="True">
                <FontIcon Glyph="&#xF228;"/>
            </ToggleButton>

            <ToggleButton Style="{StaticResource SidebarToggleButtonStyle}" ToolTipService.ToolTip="Documents">
                <FontIcon Glyph="&#xE7C3;"/>
            </ToggleButton>

            <ToggleButton Style="{StaticResource SidebarToggleButtonStyle}" ToolTipService.ToolTip="Settings">
                <FontIcon Glyph="&#xE713;"/>
            </ToggleButton>

            <ToggleButton Style="{StaticResource SidebarToggleButtonStyle}" ToolTipService.ToolTip="Reports">
                <FontIcon Glyph="&#xE74C;"/>
            </ToggleButton>
        </StackPanel>

        <ToggleButton Grid.Row="2" Style="{StaticResource SidebarToggleButtonStyle}" VerticalAlignment="Bottom" Margin="0,0,0,10" ToolTipService.ToolTip="Logout">
            <FontIcon Glyph="&#xE7E8;"/>
        </ToggleButton>
    </Grid>
</UserControl>